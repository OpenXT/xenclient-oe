# Define the hsc2hs linker so the custom Setup.hs, used by haskell packages
# binding against C libraries, from XenClient/OpenXT can use it. In this case
# it will be the wrapper generated by the haskell.bbclass containing CCLD and
# LDFLAGS for the target environment.
EXTRA_CABAL_CONF += "--with-hsc2hs-ld=${S}/ghc-ld"
